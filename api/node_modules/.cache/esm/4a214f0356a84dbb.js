let createClient;_56a‍.w("redis",[["createClient",["createClient"],function(v){createClient=v}]]);

(async () => {
  const client = createClient({
    url: 'redis://redis:6379'
  });

  client.on('error', (err) => _56a‍.g.console.log('Redis Client Error', err));

  await client.connect();

  await client.set('key', JSON.stringify({"test":{"test":["klkl",1000,"lklk"]}}) );
  const value = await JSON.parse(client.get('key'));

  _56a‍.g.console.log(value)
})();
